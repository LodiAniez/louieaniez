"use client";

import { styled } from "@mui/material/styles";
import Box from "@mui/material/Box";

const StyledBox = styled(Box)(() => ({
  display: "flex",
}));

const Wrapper = () => {
  return <StyledBox>Work in Progress.</StyledBox>;
};

export default Wrapper;
